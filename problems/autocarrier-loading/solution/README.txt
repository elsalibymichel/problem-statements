To test the functionality you can just run:

python main.py <instance_file> <solution_file>

Where <instance_file> is the path to the instance file and <solution_file> is the path to the solution file.

# Example:
python main.py instance_example.json solution_example.json